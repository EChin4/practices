float alpha = 255;
float alphaSpeed = 1;

void setup() {
  size(800, 800);
}

void draw() {
  background(255);
  noStroke();
  
  alpha += alphaSpeed;
  fill(255, 0, 0, alpha);
  rect(50, 50, 400, 600);
}

void mousePressed() {
  alphaSpeed = -1;
}
